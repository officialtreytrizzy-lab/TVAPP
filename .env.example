# Optional commercial-grade GPU video eraser worker.
# When this URL is set, the app sends the video + mask to the worker.
# When it is empty, the app falls back to the browser-only demo pipeline.
VITE_ERASER_GPU_WORKER_URL=

# Optional bearer token. Only set this if your GPU worker requires auth.
VITE_ERASER_GPU_API_KEY=
